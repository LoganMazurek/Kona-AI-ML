# Development / training / test dependencies.
# These are NOT installed in the production Docker image (see requirements.txt).
# Install locally with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

optuna>=4.8.0          # hyperparameter search, used only by source/Kona_AI_ML.py --train
matplotlib>=3.10.9      # training/diagnostic plots, lazy-imported in Kona_AI_ML.py
pytest>=9.1.1          # test suite (tests/)
